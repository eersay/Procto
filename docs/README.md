# Procto

Proctoring system with AI capabilities.

## Structure
- `backend`: Express API
- `frontend`: React + Vite
- `shared`: Shared TypeScript types
- `docker`: Docker configuration
- `docs`: Documentation

## Getting Started
1. `cd backend && npm install`
2. `cd frontend && npm install`
3. `cd shared && npm install`
